'use strict';

/**
 * answer-of-user controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::answer-of-user.answer-of-user');
