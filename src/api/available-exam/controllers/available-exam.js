'use strict';

/**
 * available-exam controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::available-exam.available-exam');
